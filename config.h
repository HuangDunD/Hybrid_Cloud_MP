#pragma once
// Compatibility shim. The canonical config header lives at common/config.h
// after master's restructure; this forwarding header keeps existing
// `#include "config.h"` call sites working.
#include "common/config.h"
